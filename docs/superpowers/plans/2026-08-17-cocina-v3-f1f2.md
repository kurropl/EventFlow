# Módulo Cocina v3 — Fases 1 y 2: Estabilización + Modelo stock/cierre — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Estabilizar el módulo Cocina (arreglar los 500 de producción/logística/servicio/guía + seed de zonas) y construir el modelo monetario/stock: disponibilidad informativa en escandallo, datos maestros proveedor×ingrediente, propuesta de OC con HITL y regularizaciones de inventario.

**Architecture:** Arreglar primero los bugs de esquema que rompen las hojas (cocinaSheets/cocinaGuia consultan columnas inexistentes), luego añadir los datos maestros de compra (supplier_ingredient_pricing) y el cálculo de disponibilidad por evento, y finalmente el flujo HITL de OCs sobre el esquema existente de `supplier_orders`.

**Tech Stack:** Next.js 14 (App Router), PostgreSQL, TypeScript, Vitest, Playwright.

## Global Constraints

- Paleta original: gold `#c9a84c`, cream `#faf8f5`, ink `#1a1a1a`, paper `#fbf8f1` — NO Tailwind default colors.
- Tipografía: Playfair Display (headings) + Inter (body). Iconos Phosphor vía `react-icons/pi` — NO emojis.
- Escala compacta `text-[10px]`-`text-[11px]`.
- **Módulo Escandallos FROZEN**: `src/app/admin/cocina/escandallos/page.tsx`, `src/app/api/cocina/escandallos/route.ts`, `src/app/api/cocina/escandallos/[escandalloId]/lines/route.ts`, `humanizeUnit` en `src/lib/units-pure.ts` — NO se modifican. La disponibilidad es un panel NUEVO que consume una API NUEVA.
- Migraciones: `db/migrations/NNN_*.sql`, idempotentes (IF NOT EXISTS).
- Tests: Vitest en `src/lib/__tests__/`; E2E en `tests/` con `playwright.prod.config.ts`.
- PG devuelve numerics como strings: SIEMPRE `Number()` antes de aritmética o `.toFixed()`.
- `recipe_items` es VISTA en prod (sobre recipe_ingredients) con `catalog_item_id` derivado.
- Estados reales de `supplier_orders.status` (check constraint actual): `pending, approved, delivered, received, partial, cancelled`.
- Autenticación: cookie `eventflow_token` / `admin_session`, `verifyToken`.

---

### Task 1: Fix bug — cocinaSheets.ts consulta `esi.category` inexistente

**Files:**
- Modify: `src/lib/cocinaSheets.ts` (líneas ~181-186, query de producción y logística)
- Test: crear `src/lib/__tests__/cocina-sheets-esquema.test.ts`

**Interfaces:**
- Consumes: `event_shopping_items` (columnas reales: `id, event_id, ingredient_name, theoretical_qty, theoretical_unit, estimated_cost, recipe_item_id`), `recipe_items` (vista, tiene `catalog_item_id`), `catalog_items` (tiene `category`).
- Produces: query corregida que devuelve `category` derivado de `catalog_items.category` vía JOIN.

**Problema:** `esi.category` y `ORDER BY esi.category` no existen. El pase de servicio se deriva de la categoría del plato, que está en `catalog_items.category`.

- [ ] **Step 1: Escribir el test de regresión (falla contra prod schema)**

```ts
// src/lib/__tests__/cocina-sheets-esquema.test.ts
import { describe, it, expect } from 'vitest';
import { getPool } from '@/lib/db';

describe('cocinaSheets — esquema real de event_shopping_items', () => {
  it('event_shopping_items NO tiene columna category (el bug es que se consultaba)', async () => {
    const res = await getPool().query(
      `SELECT count(*)::int AS n FROM information_schema.columns
       WHERE table_name='event_shopping_items' AND column_name='category'`
    );
    expect(res.rows[0].n).toBe(0); // si pasa, confirma que category no existe y hay que derivarla
  });

  it('recipe_items (vista) expone catalog_item_id para derivar la categoría', async () => {
    const res = await getPool().query(
      `SELECT count(*)::int AS n FROM information_schema.columns
       WHERE table_name='recipe_items' AND column_name='catalog_item_id'`
    );
    expect(res.rows[0].n).toBe(1);
  });
});
```

- [ ] **Step 2: Ejecutar el test**

Run: `npx vitest run src/lib/__tests__/cocina-sheets-esquema.test.ts`
Expected: PASS (2 tests) — confirma el diagnóstico.

- [ ] **Step 3: Corregir la query en cocinaSheets.ts**

En `generateProductionSheet` (y en la query paralela de logística ~línea 304), sustituir `esi.category` por un JOIN a catalog_items:

```ts
const items = await pool.query(
  `SELECT esi.id AS item_id, esi.ingredient_name, esi.theoretical_qty, esi.theoretical_unit,
          esi.estimated_cost, ci.category AS category, esi.recipe_item_id,
          ri.quantity AS recipe_qty, ri.unit AS recipe_unit
   FROM event_shopping_items esi
   LEFT JOIN recipe_items ri ON ri.id = esi.recipe_item_id
   LEFT JOIN catalog_items ci ON ci.id = ri.catalog_item_id
   WHERE esi.event_id = $1 AND esi.frozen = false
   ORDER BY ci.category, esi.ingredient_name`,
  [eventId]
);
```

Repetir el mismo patrón en la query de logística (misma estructura).

- [ ] **Step 4: Verificar contra prod**

```bash
ssh root@62.171.134.0 "cd /root/eventflow && git pull && rm -rf .next && npm run build && docker restart eventflow && sleep 10 && \
TOKEN=\$(grep eventflow_token /tmp/ef-cookies.txt | awk '{print \$NF}') && \
curl -s -o /dev/null -w 'production: %{http_code}\n' -H \"Cookie: eventflow_token=\$TOKEN\" http://localhost:3020/api/cocina/event/7b83f427-6da9-4969-9e5d-b7fe52f38ecc/production && \
curl -s -o /dev/null -w 'logistics: %{http_code}\n' -H \"Cookie: eventflow_token=\$TOKEN\" http://localhost:3020/api/cocina/event/7b83f427-6da9-4969-9e5d-b7fe52f38ecc/logistics && \
curl -s -o /dev/null -w 'service-sheet: %{http_code}\n' -H \"Cookie: eventflow_token=\$TOKEN\" http://localhost:3020/api/cocina/event/7b83f427-6da9-4969-9e5d-b7fe52f38ecc/service-sheet"
```
Expected: `production: 200`, `logistics: 200`, `service-sheet: 200` (antes 500).

- [ ] **Step 5: Commit**

```bash
git add src/lib/cocinaSheets.ts src/lib/__tests__/cocina-sheets-esquema.test.ts
git commit -m "fix(cocina): derivar category de catalog_items en hojas de producción/logística

esi.category no existe en event_shopping_items (bug rompía production,
logistics y service-sheet con 500). Ahora se obtiene vía JOIN
recipe_items.catalog_item_id → catalog_items.category. El pase de
servicio (getPassForCategory) sigue funcionando igual."
```

---

### Task 2: Fix bug — cocinaGuia.ts consulta `service_type` inexistente

**Files:**
- Modify: `src/lib/cocinaGuia.ts:84` (query de `buildCocinaGuia`)
- Test: ampliar `src/lib/__tests__/cocina-sheets-esquema.test.ts`

**Interfaces:**
- Consumes: `events` (columnas reales: `venue_type` existe, `service_type` NO).
- Produces: `buildCocinaGuia` funciona sin `service_type` — `serviceType` se deriva de `event_type` o default 'menu'.

- [ ] **Step 1: Añadir test de regresión**

```ts
it('events NO tiene service_type (el bug era consultarlo)', async () => {
  const res = await getPool().query(
    `SELECT count(*)::int AS n FROM information_schema.columns
     WHERE table_name='events' AND column_name='service_type'`
  );
  expect(res.rows[0].n).toBe(0);
});
```

- [ ] **Step 2: Corregir la query en cocinaGuia.ts**

```ts
const ev = await querySingle<any>(
  `SELECT id, client_name, event_date, guest_count, kids_count, status,
          COALESCE(venue_type,'benitez') AS venue_type,
          location, venue_pdf_url, stock_deducted
   FROM events WHERE id = $1`,
  [eventId]
);
// serviceType se deriva del tipo de evento (coctel vs menú)
const serviceType: ServiceType =
  ev.event_type === 'coctel' || ev.event_type === 'coctel-cena' ? 'coctel' : 'menu';
```

(Verificar con `grep -n "event_type" src/lib/cocinaGuia.ts` que no se usa `ev.event_type` ya; si se usa, reutilizar.)

- [ ] **Step 3: Verificar contra prod**

```bash
ssh root@62.171.134.0 "TOKEN=\$(grep eventflow_token /tmp/ef-cookies.txt | awk '{print \$NF}') && \
curl -s -o /dev/null -w 'guia: %{http_code}\n' -H \"Cookie: eventflow_token=\$TOKEN\" http://localhost:3020/api/cocina/guia/7b83f427-6da9-4969-9e5d-b7fe52f38ecc"
```
Expected: `guia: 200` (antes 500).

- [ ] **Step 4: Commit**

```bash
git add src/lib/cocinaGuia.ts src/lib/__tests__/cocina-sheets-esquema.test.ts
git commit -m "fix(cocina): derivar serviceType de event_type (service_type no existe en events)"
```

---

### Task 3: Seed kitchen_zones + verificación de rutas cocina

**Files:**
- Create: `db/migrations/033_kitchen_zones_seed.sql`
- Modify: ninguno (solo datos)

**Interfaces:**
- Consumes: tabla `kitchen_zones (id, nombre, icon, orden)` (existe, vacía en prod).
- Produces: 9 zonas de cocina pobladas.

- [ ] **Step 1: Crear la migración idempotente**

```sql
-- db/migrations/033_kitchen_zones_seed.sql
INSERT INTO kitchen_zones (nombre, icon, orden)
SELECT * FROM (VALUES
  ('aperitivos', 'wine', 1),
  ('frio', 'snowflake', 2),
  ('caliente', 'flame', 3),
  ('frito', 'fryingPan', 4),
  ('entrante', 'forkKnife', 5),
  ('primero', 'bowlSteam', 6),
  ('segundo', 'forkKnife', 7),
  ('postre', 'iceCream', 8),
  ('recena', 'moon', 9)
) AS v(nombre, icon, orden)
WHERE NOT EXISTS (SELECT 1 FROM kitchen_zones);
-- Los iconos son nombres Phosphor (no emojis) por convención del proyecto.
```

- [ ] **Step 2: Aplicar y verificar**

```bash
scp db/migrations/033_kitchen_zones_seed.sql root@62.171.134.0:/tmp/
ssh root@62.171.134.0 "docker exec -i eventflow-postgres psql -U postgres -d eventflow < /tmp/033_kitchen_zones_seed.sql && \
docker exec eventflow-postgres psql -U postgres -d eventflow -t -c 'SELECT count(*) FROM kitchen_zones;'"
```
Expected: `9`.

- [ ] **Step 3: Smoke test completo de cocina**

```bash
# Ejecutar el script /tmp/smoke-cocina.sh existente (o recrearlo con las 17 rutas)
# Verificar: 0 errores 500.
```

- [ ] **Step 4: Commit**

```bash
git add db/migrations/033_kitchen_zones_seed.sql
git commit -m "feat(cocina): seed de kitchen_zones (9 zonas, iconos Phosphor)"
```

---

### Task 4: API de disponibilidad de stock por escandallo (informativo)

**Files:**
- Create: `src/app/api/cocina/escandallos/[escandalloId]/disponibilidad/route.ts`
- Test: `src/lib/__tests__/disponibilidad-stock.test.ts` (unitario puro con pool real contra prod vía SSH, o integración)

**Interfaces:**
- Consumes: `escandallos (id, event_id, pax, total_cost)`, `escandallo_lines (escandallo_id, catalog_item_id, cantidad, unit, cost_unit)`, `recipe_ingredients (recipe_id, ingredient_id, quantity, unit)`, `ingredients (id, name, quantity, unit, min_stock)`, `inventory_commitments (event_id, ingredient_id, qty_committed)`, `stock_lots (ingredient_id, qty_base_remaining)`.
- Produces: `GET /api/cocina/escandallos/[escandalloId]/disponibilidad → { success, data: [{ ingredient_id, nombre, unidad, necesario, con_seguridad, stock, comprometido, disponible, faltante, en_alertas }] }`.

**Regla de negocio (spec §4.4):** necesario = Σ (línea de receta × pax) con margen de seguridad de `business_settings.escandallo_seguridad_pct` (default 5). stock = Σ lotes. comprometido = Σ inventory_commitments de OTROS eventos. disponible = stock − comprometido. faltante = max(0, con_seguridad − disponible).

- [ ] **Step 1: Escribir el test**

```ts
// src/lib/__tests__/disponibilidad-stock.test.ts
import { describe, it, expect } from 'vitest';
import { calcularDisponibilidad } from '@/lib/domain/disponibilidadStock';

describe('calcularDisponibilidad', () => {
  it('calcula faltante cuando el stock no cubre la necesidad con seguridad', () => {
    const res = calcularDisponibilidad({
      necesidad: 100, // g
      conSeguridad: 105, // 100 × 1.05
      stock: 80,
      comprometido: 0,
    });
    expect(res.faltante).toBe(25); // 105 - 80
    expect(res.disponible).toBe(80);
  });

  it('descuenta compromisos de otros eventos del disponible', () => {
    const res = calcularDisponibilidad({
      necesidad: 100, conSeguridad: 105, stock: 150, comprometido: 60,
    });
    expect(res.disponible).toBe(90); // 150 - 60
    expect(res.faltante).toBe(15); // 105 - 90
  });

  it('devuelve faltante 0 si hay stock suficiente', () => {
    const res = calcularDisponibilidad({
      necesidad: 100, conSeguridad: 105, stock: 200, comprometido: 0,
    });
    expect(res.faltante).toBe(0);
  });
});
```

- [ ] **Step 2: Ejecutar test (debe fallar)**

Run: `npx vitest run src/lib/__tests__/disponibilidad-stock.test.ts`
Expected: FAIL — `calcularDisponibilidad` no existe.

- [ ] **Step 3: Implementar la función pura**

```ts
// src/lib/domain/disponibilidadStock.ts
export interface DisponibilidadInput {
  necesidad: number;
  conSeguridad: number;
  stock: number;
  comprometido: number;
}
export interface DisponibilidadResult {
  necesario: number;
  con_seguridad: number;
  stock: number;
  comprometido: number;
  disponible: number;
  faltante: number;
}
export function calcularDisponibilidad(i: DisponibilidadInput): DisponibilidadResult {
  const stock = Math.max(0, Number(i.stock) || 0);
  const comprometido = Math.max(0, Number(i.comprometido) || 0);
  const conSeguridad = Math.max(0, Number(i.conSeguridad) || 0);
  const disponible = Math.max(0, stock - comprometido);
  const faltante = Math.max(0, conSeguridad - disponible);
  return {
    necesario: Number(i.necesidad) || 0,
    con_seguridad: conSeguridad,
    stock, comprometido, disponible, faltante,
  };
}
```

- [ ] **Step 4: Ejecutar test (debe pasar)**

Run: `npx vitest run src/lib/__tests__/disponibilidad-stock.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Implementar la API**

```ts
// src/app/api/cocina/escandallos/[escandalloId]/disponibilidad/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getPool, queryMany, querySingle } from '@/lib/db';
import { verifyToken } from '@/lib/auth';
import { sanitizeError } from '@/lib/security';
import { calcularDisponibilidad } from '@/lib/domain/disponibilidadStock';

export async function GET(
  request: NextRequest,
  { params }: { params: Promise<{ escandalloId: string }> }
) {
  try {
    const token = request.cookies.get('admin_session')?.value || request.cookies.get('eventflow_token')?.value;
    if (!token || !verifyToken(token)) {
      return NextResponse.json({ success: false, error: 'No autorizado' }, { status: 401 });
    }
    const { escandalloId } = await params;

    const pool = getPool();
    const esc = await pool.query(
      `SELECT id, event_id, pax FROM escandallos WHERE id = $1`, [escandalloId]
    );
    if (!esc.rows.length) return NextResponse.json({ success: false, error: 'Escandallo no encontrado' }, { status: 404 });
    const { event_id, pax } = esc.rows[0];

    // Margen de seguridad configurable
    const settings = await querySingle<any>(
      `SELECT COALESCE(escandallo_seguridad_pct, 5)::float AS pct FROM business_settings LIMIT 1`
    ).catch(() => null);
    const seguridad = Number(settings?.pct ?? 5) / 100;

    // Necesidades: del escandallo (líneas) desglosadas por ingrediente vía recipe_ingredients
    const neces = await queryMany<any>(
      `SELECT i.id AS ingredient_id, i.name, i.unit,
              SUM(ri.quantity * $2) AS necesario
       FROM escandallo_lines el
       JOIN recipe_ingredients ri ON ri.recipe_id = el.recipe_id
       JOIN ingredients i ON i.id = ri.ingredient_id
       WHERE el.escandallo_id = $1
       GROUP BY i.id, i.name, i.unit`,
      [escandalloId, pax]
    );
    // fallback: si no hay recipe_ingredients, usar el campo cantidad del escandallo
    // (verificar el mapping real de escandallo_lines → ingrediente en Task 1)

    // Stock por lotes
    const stock = await queryMany<any>(
      `SELECT ingredient_id, SUM(qty_base_remaining)::float AS stock
       FROM stock_lots GROUP BY ingredient_id`
    );
    const stockMap = new Map(stock.map((r: any) => [r.ingredient_id, Number(r.stock) || 0]));

    // Compromisos de OTROS eventos
    const comp = await queryMany<any>(
      `SELECT ingredient_id, SUM(qty_committed)::float AS comprometido
       FROM inventory_commitments WHERE event_id <> $1 GROUP BY ingredient_id`,
      [event_id]
    );
    const compMap = new Map(comp.map((r: any) => [r.ingredient_id, Number(r.comprometido) || 0]));

    const data = neces.map((n: any) => {
      const necesario = Number(n.necesario) || 0;
      return {
        ingredient_id: n.ingredient_id,
        nombre: n.name,
        unidad: n.unit,
        ...calcularDisponibilidad({
          necesidad: necesario,
          conSeguridad: necesario * (1 + seguridad),
          stock: stockMap.get(n.ingredient_id) ?? 0,
          comprometido: compMap.get(n.ingredient_id) ?? 0,
        }),
      };
    });

    return NextResponse.json({ success: true, data });
  } catch (error) {
    return NextResponse.json({ success: false, error: sanitizeError(error) }, { status: 500 });
  }
}
```

**Nota para el implementador:** verificar el mapping real de `escandallo_lines.catalog_item_id` → `recipes` → `recipe_ingredients`. Si `escandallo_lines` no tiene `recipe_id`, derivar vía `catalog_item_id` → `recipes.catalog_item_id`. Ajustar el JOIN según lo que exista (verificar en BD antes).

- [ ] **Step 6: Verificar API contra prod**

```bash
ssh root@62.171.134.0 "TOKEN=\$(grep eventflow_token /tmp/ef-cookies.txt | awk '{print \$NF}') && \
curl -s -H \"Cookie: eventflow_token=\$TOKEN\" http://localhost:3020/api/cocina/escandallos/3b162439-8301-4cc0-8e4f-51cfa13b4c93/disponibilidad | python3 -m json.tool | head -30"
```
Expected: `success: true`, array con `necesario/stock/disponible/faltante`.

- [ ] **Step 7: Commit**

```bash
git add src/lib/domain/disponibilidadStock.ts src/lib/__tests__/disponibilidad-stock.test.ts \
  src/app/api/cocina/escandallos/\[escandalloId\]/disponibilidad/route.ts
git commit -m "feat(cocina): API de disponibilidad de stock por escandallo (informativa)

Necesario vs stock (Σ lotes) vs comprometido (otros eventos) con margen
de seguridad configurable. Solo lectura — comprar se hace en Stock/Compras."
```

---

### Task 5: Panel informativo de disponibilidad en la página de escandallos

**Files:**
- Modify: `src/app/admin/cocina/escandallos/page.tsx` — SOLO AÑADIR un panel, NO tocar lógica existente (FROZEN: no modificar la tabla, edición, ni bebidas).
- Test: E2E en `tests/cocina-pasta-espejo.spec.ts` (ampliar).

**Interfaces:**
- Consumes: `GET /api/cocina/escandallos/[escandalloId]/disponibilidad` (Task 4).
- Produces: panel "Disponibilidad de stock" con semáforo por ingrediente.

- [ ] **Step 1: Añadir el fetch y el panel (solo aditivo)**

En `src/app/admin/cocina/escandallos/page.tsx`, dentro del componente, añadir estado y efecto (sin tocar lo existente):

```tsx
const [disponibilidad, setDisponibilidad] = useState<any[] | null>(null);

// en loadEscandallo, tras setEscandallo(...):
if (row?.id) {
  fetch(`/api/cocina/escandallos/${row.id}/disponibilidad`, { credentials: 'include' })
    .then(r => r.json())
    .then(d => { if (d.success) setDisponibilidad(d.data); })
    .catch(() => {});
}
```

Renderizar el panel entre las KPIs y la tabla de ingredientes (bloque nuevo, no tocar lo demás):

```tsx
{disponibilidad && disponibilidad.length > 0 && (
  <div className="bg-white rounded-lg border border-divider/50 p-3">
    <div className="flex items-center gap-2 mb-2">
      <Icon name="warning" className="w-3.5 h-3.5 text-gold" />
      <span className="text-[11px] font-medium text-ink">Disponibilidad de stock</span>
    </div>
    <div className="space-y-1">
      {disponibilidad.map((d: any) => (
        <div key={d.ingredient_id} className="flex items-center justify-between text-[10px]">
          <span className="text-ink">{d.nombre}</span>
          <span className={cn('font-medium', d.faltante > 0 ? 'text-red-600' : 'text-success')}>
            {d.faltante > 0
              ? `Faltan ${Number(d.faltante).toLocaleString('es-ES', { maximumFractionDigits: 2 })} ${d.unidad}`
              : `✓ Suficiente (disp. ${Number(d.disponible).toLocaleString('es-ES', { maximumFractionDigits: 2 })} ${d.unidad})`}
          </span>
        </div>
      ))}
    </div>
    <p className="text-[9px] text-ink-soft mt-2">
      La compra se gestiona desde Stock/Compras (informativo aquí).
    </p>
  </div>
)}
```

- [ ] **Step 2: Test E2E**

Ampliar `tests/cocina-pasta-espejo.spec.ts` con un test que seleccione el evento y compruebe que el panel aparece:

```ts
test('UI: panel de disponibilidad visible en el escandallo', async ({ page }) => {
  await loginAsAdmin(page);
  await page.goto(`${BASE}/admin/cocina/escandallos`);
  await page.waitForTimeout(2500);
  await page.locator('select').first().selectOption({ index: 1 });
  await page.waitForTimeout(3000);
  const body = await page.locator('body').innerText();
  expect(body).toContain('Disponibilidad de stock');
});
```

- [ ] **Step 3: Verificar en web + suite**

```bash
SEED_APP_URL=http://localhost:3021 NEXT_PUBLIC_APP_URL=https://eventcater.duckdns.org \
npx playwright test tests/cocina-pasta-espejo.spec.ts --config=playwright.prod.config.ts
```
Expected: todos pasan (incl. el nuevo).

- [ ] **Step 4: Commit**

```bash
git add src/app/admin/cocina/escandallos/page.tsx tests/cocina-pasta-espejo.spec.ts
git commit -m "feat(cocina): panel informativo de disponibilidad en escandallos

Aditivo (módulo FROZEN intacto): muestra necesario vs stock vs
comprometido con semáforo. La compra se gestiona en Stock/Compras."
```

---

### Task 6: Datos maestros proveedor × ingrediente

**Files:**
- Create: `db/migrations/034_supplier_ingredient_pricing.sql`
- Create: `src/app/api/providers/[id]/pricing/route.ts` (CRUD)
- Test: `src/lib/__tests__/supplier-pricing.test.ts`

**Interfaces:**
- Consumes: `providers (id, name)`, `ingredients (id, name, unit)`.
- Produces: tabla `supplier_ingredient_pricing` con precio vigente, unidad de compra, factor de conversión, pedido mínimo, lead time, proveedor preferente. API `GET/POST/PUT/DELETE /api/providers/[id]/pricing`.

- [ ] **Step 1: Migración**

```sql
-- db/migrations/034_supplier_ingredient_pricing.sql
CREATE TABLE IF NOT EXISTS supplier_ingredient_pricing (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  supplier_id uuid NOT NULL REFERENCES providers(id) ON DELETE CASCADE,
  ingredient_id uuid NOT NULL REFERENCES ingredients(id) ON DELETE CASCADE,
  precio_vigente numeric(12,4) NOT NULL,
  unidad_compra text NOT NULL DEFAULT 'caja',
  cantidad_por_unidad numeric(12,4) NOT NULL DEFAULT 1,
  unidad_uso text NOT NULL,
  factor_conversion numeric(14,4) NOT NULL,
  pedido_minimo numeric(12,4) DEFAULT 0,
  plazo_entrega_dias int DEFAULT 0,
  dias_reparto jsonb NOT NULL DEFAULT '[]'::jsonb,
  preferente boolean NOT NULL DEFAULT false,
  activo boolean NOT NULL DEFAULT true,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (supplier_id, ingredient_id)
);
CREATE INDEX IF NOT EXISTS idx_sip_ingredient ON supplier_ingredient_pricing(ingredient_id);
CREATE INDEX IF NOT EXISTS idx_sip_supplier ON supplier_ingredient_pricing(supplier_id);
```

- [ ] **Step 2: Test de la lógica de redondeo a unidad de compra**

```ts
// src/lib/__tests__/supplier-pricing.test.ts
import { describe, it, expect } from 'vitest';
import { calcularUnidadesCompra } from '@/lib/domain/supplierPricing';

describe('calcularUnidadesCompra', () => {
  it('redondea a la unidad de compra (4.7 kg → 1 caja de 6 kg)', () => {
    const res = calcularUnidadesCompra({ necesidadKg: 4.7, factorConversion: 6, pedidoMinimo: 0 });
    expect(res.unidades).toBe(1);
    expect(res.cantidadRealKg).toBe(6);
  });

  it('respeta el pedido mínimo', () => {
    const res = calcularUnidadesCompra({ necesidadKg: 2, factorConversion: 6, pedidoMinimo: 2 });
    expect(res.unidades).toBe(2); // mínimo 2 cajas
  });

  it('devuelve 0 si no hay necesidad', () => {
    const res = calcularUnidadesCompra({ necesidadKg: 0, factorConversion: 6, pedidoMinimo: 0 });
    expect(res.unidades).toBe(0);
  });
});
```

- [ ] **Step 3: Implementar la función pura**

```ts
// src/lib/domain/supplierPricing.ts
export interface CompraInput { necesidadKg: number; factorConversion: number; pedidoMinimo: number; }
export interface CompraResult { unidades: number; cantidadRealKg: number; }
export function calcularUnidadesCompra(i: CompraInput): CompraResult {
  const necesidad = Math.max(0, Number(i.necesidadKg) || 0);
  const factor = Math.max(0.0001, Number(i.factorConversion) || 1);
  const minimo = Math.max(0, Number(i.pedidoMinimo) || 0);
  if (necesidad <= 0) return { unidades: 0, cantidadRealKg: 0 };
  const unidades = Math.ceil(necesidad / factor);
  return { unidades: Math.max(unidades, minimo), cantidadRealKg: Math.max(unidades, minimo) * factor };
}
```

- [ ] **Step 4: Tests pasan**

Run: `npx vitest run src/lib/__tests__/supplier-pricing.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: API CRUD de pricing**

```ts
// src/app/api/providers/[id]/pricing/route.ts
// GET: lista supplier_ingredient_pricing del proveedor
// POST: crea fila (upsert por supplier_id+ingredient_id)
// PUT: actualiza precio/unidad/factor/minimo/lead_time/preferente
// DELETE: desactiva (activo=false, no borrado físico)
```

Implementar con los mismos patrones de auth de las demás rutas (`verifyToken`, cookie). Validar con `sanitizeText` los campos de texto y `Number()` los numéricos.

- [ ] **Step 6: Verificar en prod + commit**

```bash
# Crear pricing de prueba para Cárnicas Semilla + Ternera, verificar GET
```

```bash
git add db/migrations/034_supplier_ingredient_pricing.sql \
  src/lib/domain/supplierPricing.ts src/lib/__tests__/supplier-pricing.test.ts \
  src/app/api/providers/\[id\]/pricing/route.ts
git commit -m "feat(compras): datos maestros proveedor×ingrediente + redondeo a unidad de compra"
```

---

### Task 7: Propuesta de OC con human-in-the-loop

**Files:**
- Modify: `src/app/api/stock/supplier-orders/route.ts` (añadir estados nuevos al check)
- Create: `src/app/api/stock/necesidades/route.ts` (POST)
- Create: `src/app/api/stock/propuesta-oc/route.ts` (POST)
- Modify: `src/app/api/stock/supplier-orders/[id]/route.ts` (transiciones enviar/confirmar/recibir/cancelar) — o nueva ruta `src/app/api/stock/supplier-orders/[id]/transitions/route.ts`
- Test: `src/lib/__tests__/propuesta-oc.test.ts`

**Interfaces:**
- Consumes: `supplier_ingredient_pricing` (Task 6), `event_shopping_items` (necesidades por evento), `ingredients.supplier`, `supplier_orders/items` (esquema existente).
- Produces: `POST /api/stock/necesidades` (faltantes del evento), `POST /api/stock/propuesta-oc` (crea supplier_orders + items por proveedor), transiciones de estado.

**Ajuste al esquema real:** el check actual de `supplier_orders.status` es `('pending','approved','delivered','received','partial','cancelled')`. La propuesta (spec v3) es `propuesta/enviado/confirmado/recibido/cancelado` — mapear sin romper:
- `pending` → propuesta (estado inicial de auto-orders, se conserva)
- `approved` → enviado (pedido hecho al proveedor)
- `delivered` → confirmado (proveedor confirma — opcional)
- `received` → recibido
- `partial` → recepción parcial (backorder)
- `cancelled` → cancelado

**Decisión: mantener el check existente y usar los valores actuales** (`pending/approved/delivered/received/partial/cancelled`) para NO romper lo existente, y documentar el mapeo en la UI. (El spec v3 §5.5 pedía renombrar; aquí se conserva el enum real por compatibilidad — anotar como decisión técnica.)

- [ ] **Step 1: Migración de índices/FKs si hace falta**

Verificar que `supplier_order_items.order_id` referencia `supplier_orders.id` y que `supplier_orders.event_id` existe (sí). No requiere migración si el esquema ya lo soporta.

- [ ] **Step 2: Test de agrupación por proveedor**

```ts
// src/lib/__tests__/propuesta-oc.test.ts
import { describe, it, expect } from 'vitest';
import { agruparPorProveedor } from '@/lib/domain/propuestaOC';

describe('agruparPorProveedor', () => {
  const faltantes = [
    { ingredient_id: 'a', nombre: 'Ternera', unidad: 'g', faltante: 5000, supplier: 'Cárnicas Semilla' },
    { ingredient_id: 'b', nombre: 'Chorizo', unidad: 'g', faltante: 3000, supplier: 'Cárnicas Semilla' },
    { ingredient_id: 'c', nombre: 'Vino', unidad: 'ml', faltante: 20000, supplier: 'Bodega del Sol' },
  ];
  it('agrupa por proveedor', () => {
    const res = agruparPorProveedor(faltantes);
    expect(Object.keys(res)).toHaveLength(2);
    expect(res['Cárnicas Semilla']).toHaveLength(2);
    expect(res['Bodega del Sol']).toHaveLength(1);
  });
});
```

- [ ] **Step 3: Implementar la agrupación + propuesta**

```ts
// src/lib/domain/propuestaOC.ts
export function agruparPorProveedor(faltantes: any[]): Record<string, any[]> {
  const res: Record<string, any[]> = {};
  for (const f of faltantes) {
    const s = f.supplier || 'Sin proveedor';
    if (!res[s]) res[s] = [];
    res[s].push(f);
  }
  return res;
}
```

- [ ] **Step 4: API POST /api/stock/necesidades**

```ts
// POST body: { event_id }
// 1. Leer event_shopping_items del evento (teórico)
// 2. Para cada línea: stock (Σ lotes) − comprometidos (otros eventos) → faltante
// 3. Aplicar margen de seguridad (business_settings.escandallo_seguridad_pct)
// 4. Redondear a unidad de compra usando supplier_ingredient_pricing
//    (proveedor preferente de ingredients.supplier; si hay pricing, usar factor)
// 5. Devolver: { proveedor, items: [{ingredient_id, nombre, cantidad_pedido, unidad_compra, precio_estimado}] }
```

- [ ] **Step 5: API POST /api/stock/propuesta-oc**

```ts
// POST body: { event_id, ordenes: [{ supplier, items: [{ingredient_id, cantidad, unidad}] }] }
// 1. Por cada proveedor: INSERT supplier_orders (supplier, status='pending', event_id, origin='propuesta')
// 2. Por cada item: INSERT supplier_order_items (order_id, ingredient_id, ingredient_name, quantity, unit)
// 3. Devolver las OCs creadas (id)
```

- [ ] **Step 6: Transiciones de estado (ruta nueva)**

```ts
// src/app/api/stock/supplier-orders/[id]/transitions/route.ts
// PUT body: { accion: 'enviar'|'confirmar'|'recibir'|'cancelar' }
// - enviar:    status 'pending' → 'approved'
// - confirmar: status 'approved' → 'delivered'   (opcional, salta si proveedor no confirma)
// - recibir:   status 'approved'|'delivered' → 'received' (+ dispara APPCC/recepción y stock)
// - cancelar:  → 'cancelled' (requiere motivo)
// Validar transición; escribir audit_log.
```

- [ ] **Step 7: Verificar en prod (flujo completo)**

```bash
# 1. POST /api/stock/necesidades {event_id: <Test PASTA ESPEJO>} → faltantes
# 2. POST /api/stock/propuesta-oc → crea OC pending
# 3. PUT transitions {accion: 'enviar'} → approved
# 4. PUT transitions {accion: 'recibir'} → received (si no hay confirmación)
```

- [ ] **Step 8: Commit**

```bash
git add src/lib/domain/propuestaOC.ts src/lib/__tests__/propuesta-oc.test.ts \
  src/app/api/stock/necesidades/route.ts src/app/api/stock/propuesta-oc/route.ts \
  src/app/api/stock/supplier-orders/\[id\]/transitions/route.ts
git commit -m "feat(compras): necesidades por evento + propuesta de OC + transiciones HITL"
```

---

### Task 8: Regularizaciones de inventario

**Files:**
- Create: `db/migrations/035_inventory_adjustments.sql`
- Create: `src/app/api/stock/regularizaciones/route.ts` (POST listar/crear)
- Test: `src/lib/__tests__/regularizaciones.test.ts`

**Interfaces:**
- Consumes: `ingredients (id, quantity)`, `stock_lots`.
- Produces: tabla `inventory_adjustments`, API para registrar recuentos/roturas/mermas/caducados con motivo y responsable, ajustando stock vía `adjustIngredientStock` (ledger único, ya existe).

- [ ] **Step 1: Migración**

```sql
-- db/migrations/035_inventory_adjustments.sql
CREATE TABLE IF NOT EXISTS inventory_adjustments (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  ingredient_id uuid REFERENCES ingredients(id),
  ajuste numeric NOT NULL,
  tipo text NOT NULL CHECK (tipo IN ('recuento','rotura','merma','caducado','sobrante','ajuste')),
  motivo text,
  responsable uuid REFERENCES admins(id),
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_inv_adj_ingredient ON inventory_adjustments(ingredient_id);
```

- [ ] **Step 2: Test**

```ts
// src/lib/__tests__/regularizaciones.test.ts
import { describe, it, expect } from 'vitest';
import { validarAjuste } from '@/lib/domain/regularizaciones';

describe('validarAjuste', () => {
  it('rechaza ajuste 0', () => { expect(validarAjuste(0, 'recuento')).toBe(false); });
  it('acepta ajuste negativo con tipo válido', () => { expect(validarAjuste(-5, 'rotura')).toBe(true); });
  it('acepta sobrante positivo', () => { expect(validarAjuste(3, 'sobrante')).toBe(true); });
});
```

- [ ] **Step 3: Implementar + API**

```ts
// src/lib/domain/regularizaciones.ts
export function validarAjuste(ajuste: number, tipo: string): boolean {
  if (!Number.isFinite(ajuste) || ajuste === 0) return false;
  return ['recuento','rotura','merma','caducado','sobrante','ajuste'].includes(tipo);
}
```

```ts
// src/app/api/stock/regularizaciones/route.ts
// GET: lista (filtro por ingrediente/fecha)
// POST: { ingredient_id, ajuste, tipo, motivo } → validar → adjustIngredientStock(delta=ajuste, reason='regularizacion', movementType='adjustment') → INSERT inventory_adjustments
```

- [ ] **Step 4: Verificar + commit**

```bash
git add db/migrations/035_inventory_adjustments.sql src/lib/domain/regularizaciones.ts \
  src/lib/__tests__/regularizaciones.test.ts src/app/api/stock/regularizaciones/route.ts
git commit -m "feat(compras): regularizaciones de inventario (recuento/rotura/merma/caducado)"
```

---

### Task 9: Verificación final de las fases 1 y 2

**Files:**
- Modify: `tests/cocina-pasta-espejo.spec.ts` (ampliar si falta)
- Modify: `docs/CHANGELOG-olas.md` (nueva entrada)

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: suite completa verde + CHANGELOG.

- [ ] **Step 1: Suite unitaria completa**

```bash
npx vitest run src/lib/__tests__/units-humanize.test.ts src/lib/__tests__/recipe-import.test.ts \
  src/lib/__tests__/rbac.test.ts src/lib/__tests__/disponibilidad-stock.test.ts \
  src/lib/__tests__/supplier-pricing.test.ts src/lib/__tests__/propuesta-oc.test.ts \
  src/lib/__tests__/regularizaciones.test.ts
```
Expected: PASS.

- [ ] **Step 2: Suite E2E completa**

```bash
SEED_APP_URL=http://localhost:3021 NEXT_PUBLIC_APP_URL=https://eventcater.duckdns.org \
npx playwright test tests/cocina-navigation.spec.ts tests/cocina-pasta-espejo.spec.ts \
  tests/traceability-seed.spec.ts --config=playwright.prod.config.ts
```
Expected: 19+ tests PASS.

- [ ] **Step 3: Smoke test de rutas cocina (0 errores 500)**

Ejecutar el script de smoke de las 17 rutas de cocina → todas 200.

- [ ] **Step 4: Actualizar CHANGELOG**

```markdown
### COCINA-V3-F1F2 — Estabilización + modelo stock/cierre (2026-08-17)
- Fixes 500: production/logistics/service-sheet (esi.category), guia (service_type).
- Seed kitchen_zones (9 zonas).
- API disponibilidad de stock por escandallo (informativa, FROZEN intacto).
- Datos maestros proveedor×ingrediente + redondeo unidad compra.
- Propuesta OC con HITL + transiciones (enviar/confirmar/recibir/cancelar).
- Regularizaciones de inventario.
- Decisión técnica: enum supplier_orders conservado (pending/approved/delivered/
  received/partial/cancelled) por compatibilidad — mapeo documentado en UI.
```

- [ ] **Step 5: Commit**

```bash
git add docs/CHANGELOG-olas.md
git commit -m "docs: COCINA-V3-F1F2 — estabilización + modelo stock/cierre"
```

---

## Self-Review

**Spec coverage (spec v3):**
- §3 Recetas: NO cubierto en este plan (Fase 4 del spec — sub-recetas, alérgenos, variantes). Se aborda en plan posterior. ✅ correcto, el usuario pidió Fases 1-2.
- §4 Escandallos: Tasks 4-5 (disponibilidad informativa). Pax por tipo/margen de seguridad: el margen está en Task 4; pax por tipo queda para fase posterior. ✅ parcial (anotado).
- §5 Stock/Compras: Tasks 6-7-8 (datos maestros, propuesta OC HITL, regularizaciones). Recepción parcial/backorder y rechazo en muelle: quedan para Fase 3 (no en este plan). ✅ parcial.
- §6 Consumo stock: **YA EXISTE** (stockDeduct + consumeLotsFEFO verificado en código). El plan lo documenta en Task 1/4 sin reimplementarlo. ✅ corregido vs spec.
- §7 Cierre 2 etapas: criterio de cobro documentado; la implementación del check OPC-5 queda para Fase 3 (finanzas). ✅ parcial.
- §8-10 Producción/Carga/APPCC: fases posteriores del spec. ✅ fuera de alcance de este plan.

**Placeholder scan:** El JOIN de Task 4 tiene una nota explícita de verificación (el mapping escandallo_lines→ingrediente puede variar) — es una verificación necesaria de esquema real, no un placeholder; el implementador debe confirmar en BD. ✅

**Type consistency:** `calcularDisponibilidad`, `calcularUnidadesCompra`, `agruparPorProveedor`, `validarAjuste` — nombres usados en tests y código consistentes. ✅
